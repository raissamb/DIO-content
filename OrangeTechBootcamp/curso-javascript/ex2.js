/*

class Car {
    mark;
    color;
    carFuelRangeKm;

    constructor(mark, color, carFuelRangeKm) {
        this.mark = mark;
        this.color = color;
        this.carFuelRangeKm = carFuelRangeKm;
    }

    calculateTravelCost(distanceKm, fuelPrice) {
        let travelCost = (distanceKm * this.carFuelRangeKm * fuelPrice);
        return travelCost
    }
}

const uno = new Car("Fiat", "Red", (1 / 12));
console.log(uno);
console.log(uno.calculateTravelCost(70, 5));


class Person {
    name;
    weightKg;
    heightMeters;

    constructor(name, weightKg, heightMeters) {
        this.name = name;
        this.weightKg = weightKg;
        this.heightMeters = heightMeters;
    }

    calculateIMC() {
        const imcResult = this.weightKg / (this.heightMeters * this.heightMeters);
        return imcResult;

    }

    classifyIMC() {
        const imc = this.calculateIMC();

        if (imc >= 40) {
            return ("Muito Obeso")
        } else if ((imc < 40) && (imc >= 30)) {
            return ("Obeso")
        } else if ((imc < 30) && (imc >= 25)) {
            return ("acima do peso")
        } else if ((imc < 25) && (imc >= 18.5)) {
            return ("Peso normal")
        } else {
            return ("abaixo do peso")
        }

    }
}

*/









